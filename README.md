# n-Layer-Model for Electrical Machines

**INFO**: 

Spyder: to run launchers that are not inside of the projects root directory,  
select the root directory in ```Tools -> PYTHONPATH manager -> add path```
